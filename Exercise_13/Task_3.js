// Iterate over an array using forEach and display each element.

const arr = ["gorilla", "lion", "peacock", "tiger", "chimpanzee"];

arr.forEach((item, index) => {
  console.log("The Element at index", index, "is", item);
});
