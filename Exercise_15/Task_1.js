// Create a new array that combines two existing arrays using the spread operator.

const arr1 = [1, 2, 3, 4, 5];
const arr2 = [6, 7, 8, 9, 10];
const newArr = [...arr1, ...arr2];
console.log("The new Array would look like this", newArr);
