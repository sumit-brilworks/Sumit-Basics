// Use map to create a new array with each number doubled.

const arr = [1, 2, 3, 4, 5, 6, 7, 8];
const newArr = arr.map((item) => item * 2);

console.log("The doubled number array would look like this", newArr);
